---
title: cn() Class Utility
description: Merge Tailwind class names conditionally, resolving conflicts. The standard utility used by shadcn/ui.
category: snippets
language: typescript
tags: [typescript, tailwind, utils, shadcn]
---

Combines `clsx` for conditional logic with `tailwind-merge` for resolving Tailwind class conflicts (e.g. `p-2` overriding `p-4`).

**Install dependencies first:**

```bash
npm install clsx tailwind-merge
```

```typescript
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

## Usage

```typescript
import { cn } from '@/lib/utils'

// Resolves conflicts: 'px-4' wins over 'px-2'
cn('px-2 py-1', 'px-4')           // → 'py-1 px-4'

// Conditional classes
cn('base-class', isActive && 'active', { 'hidden': !visible })
```
